{# empty Twig template #}
list of cars!!!

<table>
{% for car in carList %}
    <tr>
        <td>{{ car.id }}</td>
        <td><a href="show/{{ car.id }}">{{ car.make.name }} {{ car.model.name }}</a></td>
        <td></td>
    </tr>
{% endfor %}
</table>